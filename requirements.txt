# Core dependencies
arxiv==2.1.0
anthropic>=0.40.0
httpx>=0.27.0
jinja2==3.1.4

# Python standard library equivalents (no install needed, but documenting)
# - smtplib (stdlib)
# - sqlite3 (stdlib)
# - logging (stdlib)

# Development/Testing
pytest==8.0.0
pytest-cov==4.1.0
