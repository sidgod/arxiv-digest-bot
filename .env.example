# arXiv Digest Bot Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Claude API Configuration
# =============================================================================
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Claude model to use for summarization
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Maximum tokens per paper summary (150 = ~2-3 sentences)
SUMMARY_MAX_TOKENS=150

# =============================================================================
# arXiv Search Parameters
# =============================================================================
# arXiv categories to search (comma-separated)
# Common categories:
#   cs.AI  - Artificial Intelligence
#   cs.CL  - Computation and Language
#   cs.LG  - Machine Learning
#   cs.CV  - Computer Vision
#   cs.NE  - Neural and Evolutionary Computing
ARXIV_CATEGORIES=cs.AI,cs.CL,cs.LG

# Papers to fetch per daily ingest
ARXIV_DAILY_FETCH_LIMIT=15

# Papers to show in weekly digest email
ARXIV_DISPLAY_LIMIT=15

# Optional: Additional search query (leave empty for category-only search)
# Example: ARXIV_SEARCH_QUERY=LLM OR "large language model" OR transformer
ARXIV_SEARCH_QUERY=

# =============================================================================
# Interest-Based Ranking (Optional)
# =============================================================================
# Keywords for ranking papers (comma-separated, case-insensitive)
# Papers matching these keywords will appear first in the digest
# Leave empty to sort chronologically
# Example: INTEREST_KEYWORDS=RLHF,fine-tuning,RAG,attention,prompt engineering,agents
INTEREST_KEYWORDS=

# =============================================================================
# Email Configuration - SMTP Settings
# =============================================================================
# Gmail example (requires App Password):
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   Get App Password: https://myaccount.google.com/apppasswords
#
# Other providers:
#   Outlook: smtp-mail.outlook.com:587
#   Yahoo: smtp.mail.yahoo.com:587
#   Custom SMTP: your-smtp-server.com:587

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
EMAIL_FROM=your-email@gmail.com

# =============================================================================
# Email Configuration - Recipients
# =============================================================================
# Digest recipients (gets weekly paper summaries)
# Supports multiple recipients (comma-separated)
# Examples:
#   Single: EMAIL_TO=you@example.com
#   Multiple: EMAIL_TO=you@example.com,friend1@example.com,friend2@example.com
EMAIL_TO=your-email@example.com

# Subject line prefix for digest emails (weekly paper summaries)
EMAIL_SUBJECT_PREFIX=[arXiv Digest]

# Admin notification recipient (gets error alerts + success summaries)
# Can be same as EMAIL_TO or different
# Leave empty to use first EMAIL_TO recipient
NOTIFICATION_EMAIL_TO=

# Subject line prefix for notification emails (errors + success)
# Different from digest prefix to enable mailbox filtering/routing
NOTIFICATION_EMAIL_PREFIX=[arXiv Bot]

# Enable/disable admin notification emails (errors + success summaries)
# Set to false if you only want digest emails, no admin notifications
NOTIFICATIONS_ENABLED=true

# =============================================================================
# Storage Configuration
# =============================================================================
# Directory for database and logs (inside container)
# This is mapped to ./data on the host via docker-compose
DATA_DIR=/app/data
